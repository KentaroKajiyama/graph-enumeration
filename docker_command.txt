docker run --rm -v "$PWD:/work" -v cargo-registry:/opt/cargo/registry -v cargo-target:/work/target -w /work clux/muslrust:stable sh -lc 'apt-get update && apt-get install -y --no-install-recommends clang llvm-dev libclang-dev pkg-config ca-certificates && export LIBCLANG_PATH=$(llvm-config --libdir) && cargo build --release && cp target/x86_64-unknown-linux-musl/release/graph-enumeration dist/graph-enumeration-2'